{
  "name": "json-server-backend",
  "version": "1.0.0",
  "description": "A simple JSON Server backend for TeamFlow",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  }
}
